import React from "react";
import ReactDOM from "react-dom/client";
import MaynerLogger from "./MaynerLogger.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <MaynerLogger />
  </React.StrictMode>
);
